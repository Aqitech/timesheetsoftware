<?php

namespace App\Console\Commands;

use Illuminate\Console\Command;

class CheckUserShiftTime extends Command
{
    /**
     * The name and signature of the console command.
     *
     * @var string
     */
    protected $signature = 'app:check-user-shift-time';

    /**
     * The console command description.
     *
     * @var string
     */
    protected $description = "The task is to write a Laravel code that checks a user's shift time, creates an entry in the database if it matches";

    /**
     * Execute the console command.
     */
    public function handle()
    {
        //
    }
}
